<h1><%= @post.title %></h1>

 <div class="row">
   <div class="col-sm-5">
     <p>
       <%= @post.created_at %>
     </p>
     <%= @post.content %>
  </div>

  <div class="col-sm-7">
  </div>
 </div>
